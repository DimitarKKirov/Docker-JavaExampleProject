Create Testing Environment with Docker for a locale use.

Tools

Docker for Windows - Software containerization platform.
Available at - https://www.docker.com/products/docker-desktop

Docker setup:

After (registering in docker.com) downloading and isntalling docker desktop,
open docker UI (inside the trya) and go to: 
Settings (the cogweel on the top)->Generel (the top tab) -> check the option Expose daemon on tcp://localhost:2375 without TLS
